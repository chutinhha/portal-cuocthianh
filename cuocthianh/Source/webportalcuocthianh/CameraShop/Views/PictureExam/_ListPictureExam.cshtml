@model List<CoreData.PictureExam>
@if (Model != null && Model.Count() > 0)
{
    foreach (var item in Model)
    {
    <li><a class="tooltip" href="#" title="Post Format: Standart">
        <img src="/Media/PictureExam/@item.Image" width="68" height="68" alt="Post Format: Standart">
    </a></li>
    }
}